apply plugin: 'com.android.application'

android {
    compileSdkVersion 27

    defaultConfig {
        applicationId 'com.bubenheimer.firedance'
        minSdkVersion 24
        targetSdkVersion 27
        versionName '1.0'
        versionCode 1
        resConfigs 'en', 'en-rUS'
    }
}

dependencies {
    implementation 'com.google.android.gms:play-services-gcm:11.8.0'
}
